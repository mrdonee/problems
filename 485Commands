Bash Commands
    cmd + k                         //clears all terminal output
    ctrl + a                        //moves cursor to beginning of bash command
    ctrl + l                        //scrolls terminal until screen cleared
    python -m SimpleHTTPServer      //starts SimpleHTTPServer
    python !$                       //grabs file from previous command and runs it

Linux Commands
    cat
    curl    downloads a file and dumps the content to the command line
    grep    search
    less
    wc -l   how many lines are in a file
    wget    download a single webpage from the command line
